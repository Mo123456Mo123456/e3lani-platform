import {router} from "expo-router"; import {FlatList,StyleSheet,View} from "react-native"; import {AdCard} from "@/components/e3lani/ad-card"; import {EmptyState,ScreenTitle} from "@/components/e3lani/ui"; import {ScreenContainer} from "@/components/screen-container"; import {useE3lani} from "@/lib/e3lani-store"; import {useI18n} from "@/lib/i18n";
export default function Saved(){const{ads,savedIds,user}=useE3lani(),{t}=useI18n(),items=ads.filter(a=>savedIds.includes(a.id));return <ScreenContainer className="px-4"><ScreenTitle title={t("saved")}/>{!user?<EmptyState icon="lock" title={t("signIn")} text={t("loginHelp")} actionLabel={t("signIn")} onAction={()=>router.push("/login" as never)}/>:items.length===0?<EmptyState icon="bookmark-border" title={t("emptySaved")} text={t("save")}/>:<FlatList data={items} keyExtractor={a=>a.id} contentContainerStyle={s.list} renderItem={({item})=><View style={s.item}><AdCard ad={item} height={430}/></View>}/>}</ScreenContainer>}
const s=StyleSheet.create({list:{paddingTop:15,paddingBottom:30},item:{marginBottom:14}})
