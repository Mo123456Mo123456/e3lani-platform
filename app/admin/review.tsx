import Moderation from "./moderation";

/**
 * Backward-compatible alias for the canonical admin moderation route.
 * The admin navigation continues to use /admin/moderation.
 */
export default Moderation;
