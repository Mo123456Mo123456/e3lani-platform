FROM node:22-bookworm-slim AS app

WORKDIR /app

RUN corepack enable

COPY . .

RUN pnpm install --no-frozen-lockfile \
  && pnpm --filter @planet/admin build

ENV NODE_ENV=production
EXPOSE 3001

CMD ["pnpm", "--filter", "@planet/admin", "start"]
