FROM node:22-bookworm-slim AS app

WORKDIR /app

RUN corepack enable

COPY . .

RUN pnpm install --no-frozen-lockfile \
  && pnpm --filter @planet/db generate \
  && pnpm --filter @planet/api build

ENV NODE_ENV=production
EXPOSE 4000

CMD ["pnpm", "--filter", "@planet/api", "start"]
