from app.providers.sandbox import SandboxProvider


class GeminiAdapter(SandboxProvider):
    """Gemini provider placeholder with sandbox-safe structured behavior."""

    provider_name = "gemini"
    is_sandbox = False
