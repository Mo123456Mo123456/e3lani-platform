# Copy to .env.staging before building a standalone staging Release APK.
# Values are baked into the JS bundle at `expo export:embed` time.
EXPO_PUBLIC_API_BASE_URL=https://YOUR_STAGING_API_HOST/api/v1
EXPO_PUBLIC_APP_ENV=staging
