{
  "name": "@living-planet/simulation-models",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run tests/simulation.test.ts"
  },
  "dependencies": {
    "@living-planet/shared-types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vitest": "latest"
  }
}
