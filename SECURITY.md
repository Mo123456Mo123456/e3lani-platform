# سياسة الأمان

## الإبلاغ

هذا مستودع خاص. أبلغ مالك المستودع عن الثغرات عبر قناة خاصة، ولا تفتح Issue عامة تتضمن استغلالًا أو رمز وصول أو بيانات مستخدم. لا تُرفق ملفات قاعدة بيانات أو روابط وسائط موقعة أو سجلات جلسات في البلاغ.

## حدود المستودع

يحتوي GitHub على الشفرة والمخططات والمهاجرات والاختبارات والوثائق فقط. تبقى البيانات الفعلية في قاعدة البيانات، والملفات في التخزين السحابي، والأسرار في مدير أسرار بيئة التشغيل. يُحظر حفظ أرقام الجوال ورموز OTP والكوكيز ومفاتيح الدفع واتصالات قاعدة البيانات الحقيقية في Git.

## ضوابط إلزامية

| المجال | الضابط |
|---|---|
| الأسرار | ملف `config/environment.example` أسماء وقيم وهمية فقط؛ كل القيم الحقيقية خارج Git. |
| المصادقة | انتهاء للجلسات والرموز، وحد محاولات، وأدوار خادمية برفض افتراضي. |
| الوسائط | تحقق من MIME والتوقيع والحجم والملكية، ومفاتيح غير متوقعة، وروابط قصيرة العمر. |
| الدفع | معطل افتراضيًا؛ لا تفعيل دون Webhook موقع وIdempotency ومطابقة المبلغ والعملة. |
| السجلات | لا أرقام جوال ولا رموز ولا أسرار ولا روابط موقعة. |
| التبعيات | قفل `pnpm-lock.yaml`، ومراجعة تحديثات التبعيات، وتشغيل CI قبل الدمج. |

عند الاشتباه بتسرب سر، أوقف الاعتماد فورًا ودوّره، ثم امسح تاريخ Git عند الضرورة؛ حذف الملف من Commit جديد وحده لا يكفي.
